<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>Edit Bag</title>
    <link rel="stylesheet" href="/stylesheet/edit.css">
</head>

<body>
    <div class="container">
        <h1>Edit Bag</h1>

        <form id="editBagForm" method="post" action="/admin/bag/<%= getBag._id %>">
            <div class="form-group">
                <label for="name">Bag Name</label>
                <input type="text" id="name" value="<%= getBag.bagName %>" name="bagName">
            </div>
            <div class="form-group">
                <label for="price">Price ($)</label>
                <input type="number" id="price" value="<%= getBag.price %>" name="price">
            </div>
            <div class="form-group">
                <label for="image">Image URL</label>
                <input type="text" id="image" value="<%= getBag.imgurl %>" name="imgurl">
            </div>
            <input type="submit" value="Update Bag">
        </form>
    </div>
</body>

</html>